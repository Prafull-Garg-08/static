name: Testing github workflow features.

on: push

jobs:
  print:
    runs-on: ubuntu-slim
    environment: test
    steps:
      - name: print actor
        run: echo $GITHUB_ACTOR
      - name: environment
        run: echo "environment"